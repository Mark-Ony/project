# E-Commerce Frontend

This is a [Next.js](https://nextjs.org) e-commerce frontend application that connects to an Express backend.

## Prerequisites

- Node.js (version 18 or higher recommended)
- npm or yarn package manager

## Installation & Setup

1. Clone the repository:
```bash
git clone https://github.com/Mark-Ony/project.git
cd <project-folder>

## Navigate to the frontend directory and then to ecommerce:
cd frontend
cd ecommerce

Create a .env file at the root of your project and add the following environment variable:

env
NEXT_PUBLIC_API_URL=http://localhost:5000

Install dependencies:
npm install

run server by 
npm run dev

goodluck
by mark dev
